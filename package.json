{
  "name": "termchat",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/server"
  ],
  "scripts": {
    "server": "npm run start:dev --workspace=apps/server",
    "server:prod": "npm run start:prod --workspace=apps/server",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down -v"
  },
  "devDependencies": {
    "axios": "^1.17.0",
    "socket.io-client": "^4.8.3"
  }
}
